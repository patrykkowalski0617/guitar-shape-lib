import { element } from "@/components/ui";
import styled from "styled-components";

export const Wrapper = styled.div`
  width: calc(${element.buttonUnit} * 5);
  display: flex;
  flex-direction: row;
  justify-content: space-between;
`;
