import styled from "styled-components";

export const Sign = styled.header`
  font-weight: 100;
  font-style: italic;
  font-size: 16px;
  color: color-mix(in oklab, var(--secondary) 80%, var(--secondary-foreground));
  text-align: right;
  width: 100%;
  padding: 5px 15px;
  margin: auto;
`;
